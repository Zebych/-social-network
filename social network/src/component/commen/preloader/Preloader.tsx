import React from 'react';
import preloader from '../../../assets/images/Preloader.gif';

const Preloader = () => {
    return (
        <div>
            <img src={preloader}/>
            </div>
    );
};

export default Preloader;